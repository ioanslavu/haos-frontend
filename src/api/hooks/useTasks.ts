/**
 * @file useTasks.ts
 * @deprecated Import from '@/api/hooks/tasks' instead
 *
 * This file re-exports all task hooks for backward compatibility.
 * The hooks have been split into the tasks/ directory for better organization.
 */

// Re-export everything from the tasks module for backward compatibility
export * from './tasks';
