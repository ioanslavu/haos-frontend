/**
 * @file useUsers.ts
 * @deprecated Import from '@/api/hooks/users' instead
 *
 * This file re-exports all user hooks for backward compatibility.
 * The hooks have been split into the users/ directory for better organization.
 */

// Re-export everything from the users module for backward compatibility
export * from './users';
